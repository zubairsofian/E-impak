rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Direct request from user to allow access until May 2026
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 5, 20);
    }
  }
}
